DATABASE_URL="postgresql://user:password@localhost:5432/filattente?schema=public"
PORT=3000

# Secrets JWT - GENERER UN VRAI SECRET EN PRODUCTION
JWT_SECRET="change-me-in-production"
JWT_EXPIRES_IN="8h"

# Le SuperAdmin vit dans la table "User" (role SUPER_ADMIN, companyId NULL)
# -> créer via le seed ou un script dédié.
